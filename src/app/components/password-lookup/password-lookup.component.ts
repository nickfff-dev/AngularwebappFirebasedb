import { Component, OnInit } from '@angular/core';

@Component({
  selector: 'app-password-lookup',
  templateUrl: './password-lookup.component.html',
  styleUrls: ['./password-lookup.component.css']
})
export class PasswordLookupComponent implements OnInit {

  constructor() { }

  ngOnInit(): void {
  }

   // TODO
    // Ham to write a method that takes the email and goes to data table to look up if it is there
    // display errors on form if user doesn't enter an email
    // style the page so it doesn't look like shit

}
